### Start socket server
    `cd backend`
##### install packages
    `composer install`
##### start server
    `php bin/server.php`

### Start client
    `cd frontend`
##### install packages
    `npm install`
 
     `npm start`

Open http://localhost:3000/ in two separate tabs, and start chatting.....  
  

